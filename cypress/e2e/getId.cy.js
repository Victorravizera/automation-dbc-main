/// <reference types="cypress" />

describe('seach by products for id case tests', () => {
    it('search by product', () => {
      cy.getId()
    })
    it('search by product id fail', () => {
        cy.idFail()
      })
  })
  
